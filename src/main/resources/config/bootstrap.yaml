spring:
  application:
    name: demo-service

  cloud:
    kubernetes:
      reload:
        enabled: false

      discovery:
        enabled: true

---
spring:
  config:
    activate:
      on-profile: standalone, docker

  cloud:
    kubernetes:
      enabled: false
